To run the file navigate to it's location in the Terminal. 
Once there run "mysql -u root -p < recipes_db.sql" in the terminal. 
After, run "npm install"
Once done run "node app.js"
The terminal should now say "Server running on port 3000 MySQL Connected"
Go to your browser and paste in "http://localhost:3000"
Enjoy
